@model IEnumerable<Link2Web.Models.LinkType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Visible)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Visible)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LinkTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LinkTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LinkTypeId })
        </td>
    </tr>
}

</table>
