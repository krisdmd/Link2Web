@model IEnumerable<Link2Web.Models.WebsiteType>

@{
    ViewBag.Title = "Index";
}

<h2>Website types</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Visible)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Visible)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WebsiteTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.WebsiteTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WebsiteTypeId })
        </td>
    </tr>
}

</table>
