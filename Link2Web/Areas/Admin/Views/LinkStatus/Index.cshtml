@model IEnumerable<Link2Web.Models.LinkStatus>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Visible)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Visible)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LinkStatusId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LinkStatusId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LinkStatusId })
        </td>
    </tr>
}

</table>
